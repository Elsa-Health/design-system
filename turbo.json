{
	"pipeline": {
		"site:build": {
			"dependsOn": ["^build", "^build-storybook"],
			"outputs": ["storybook-static/**", ".next/**"]
		},
		"lint": {
			"outputs": []
		},
		"dev": {
			"cache": false
		}
	}
}
